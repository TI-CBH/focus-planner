# PocketBase API URL (as seen by the browser)
# In production with a reverse proxy, use your public domain:
# VITE_PB_URL=https://focus-api.example.com
VITE_PB_URL=http://localhost:8090

# Port mappings (host:container)
FRONTEND_PORT=8080
PB_PORT=8090
